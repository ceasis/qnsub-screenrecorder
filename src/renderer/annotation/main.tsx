import React from 'react';
import { createRoot } from 'react-dom/client';
import Annotation from './Annotation';
import './style.css';

createRoot(document.getElementById('root')!).render(<Annotation />);
